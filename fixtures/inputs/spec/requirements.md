# Order requirements

- REQ-001: The service MUST list orders at `GET /orders`.
- REQ-002: The service MUST accept order creation at `POST /orders`.
- REQ-003: A paid order MUST NOT return to pending.
